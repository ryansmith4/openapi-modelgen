rootProject.name = 'plugin'

// Share version catalog from root project
dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("../gradle/libs.versions.toml"))
        }
    }
}